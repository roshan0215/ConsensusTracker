services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: consensustracker
    ports:
      - "5432:5432"
    volumes:
      - consensustracker_pgdata:/var/lib/postgresql/data

volumes:
  consensustracker_pgdata:
